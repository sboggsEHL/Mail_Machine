import { ParameterDefinition } from '../types';

/**
 * Static definitions for basic API parameters specific to the FirstAmerican provider.
 * TODO: Populate with actual FirstAmerican parameters when implemented.
 */
export const parameters: ParameterDefinition[] = [];