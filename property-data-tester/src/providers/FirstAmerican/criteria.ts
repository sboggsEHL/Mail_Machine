import { CriteriaDefinition } from '../types';

/**
 * Static criteria definitions specific to the FirstAmerican provider.
 * TODO: Populate with actual FirstAmerican criteria definitions when implemented.
 * Note: If FirstAmerican criteria are also fetched dynamically, this file might remain empty
 * or be used for UI-specific configurations only.
 */
export const criteria: CriteriaDefinition[] = [];